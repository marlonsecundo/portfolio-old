import { colors } from 'src/styles';
import styled from 'styled-components';

export const Container = styled.ul`
  background: ${colors.card};

  padding: 1rem;

  flex: 0.3;
`;

export const WorkSelectorItem = styled.li``;
